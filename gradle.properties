org.gradle.parallel = true
org.gradle.caching = true
kotlin.code.style = official

# Patch bundle version — embedded into the .rvp manifest ("Version" attribute).
# 1.0.0: scaffold + version-field smoke test. 1.1.0: adds "Spoof signature".
version = 1.1.0
